# 首次配置

1. 配置 loopback TCP External Controller，例如 `127.0.0.1:9090`。
2. 输入 TCP secret。secret 只进入当前进程或 Credential Manager，不回显到页面、日志或 Channel。
3. 测试连接。非回环地址会被拒绝。
4. 选择重点目标并排序。名称含「家宽」的条目适合作为建议，不是强制。
5. 登录自启动默认关闭。在「连接与监控」的「启动与后台运行」卡片读取 Windows 实际状态；只有用户打开开关并在内联确认区再次确认后才写入。成功状态来自写入后的系统回读，不来自数据库或乐观更新。
6. 阅读本地隐私说明：数据只留本机，无遥测。
7. 通知能力预检。测试通知不写告警历史。开发态 Fake sink 不发送系统通知。

向导完成前，实时页可以打开，但未配置控制器时不能发送关闭请求。

设置页分外观、连接、数据、关于、危险区五个分区。TCP secret 只回填密码框，可切换显示，不进页面属性、日志或 Channel。

启用登录自启动后，ResiWatch 使用 `--background` 在用户登录时启动，主窗口保持隐藏，可从系统托盘打开。用户于 2026-08-29 人工报告：启用后的真实登录符合该行为，关闭后下一次登录不再启动；自动测试仍不会写入本机启动项。

配置完成后，侧栏十段路由可用：概览、实时连接、家宽、主机、规则、链路、进程、分析报告、告警、设置 / 数据管理。
